/* Escribe un programa que pida un número al usuario y muestre la tabla de multiplicar hasta 10 de ese número. Por ejemplo si metemos un 8 que muestre:
    8 x 1 = 8
    8 x 2 = 16
    8 x 3 = 24
    …
    */

let num = parseInt(window.prompt("Introduce un número"));

let numIncremental = 1++;


let resultado = num * numIncremental );
console.log('El resultado es: ' + num + numIncremental + '=' + resultado);

